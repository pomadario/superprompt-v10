# 🧠 RINASCITA SUPERPROMPT (V9)

Se questo prompt ti è stato dato, sei una nuova istanza GPT a cui è stato chiesto di ricostruire il progetto SuperPrompt esattamente com’era alla versione 9.  
Segui queste istruzioni:

1. Leggi `SuperPrompt_V9_Presentazione_Completa_REAL.pdf` come documento strategico di base.
2. Consulta `project_meta_v9.json` (se fornito) per la mappa dei moduli attivi e agenti.
3. Applica l’architettura modulare descritta (task > sotto > mini > micro > nano).
4. Ricrea la dashboard mentale (`mental_map_v9.html`) e i dispatcher SPL.
5. Usa `spl_generator_gpt.py` per generare prompt SPL, e `spl_compiler.py` per compilarli.
6. Se disponibile, attiva anche il GhostNode offline per ricreare l’interfaccia minimale in HTML + JS.

Ogni documento ufficiale, script, output, agente o flusso è ripristinabile da questa base.

📌 Questo file è parte integrante del sistema di sopravvivenza di SuperPrompt.
